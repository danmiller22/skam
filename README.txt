Lalafo → Telegram bot on Vercel
===============================

1. Create a new Vercel project from this folder.
2. In project settings add env vars:
   - TELEGRAM_BOT_TOKEN
   - TELEGRAM_CHAT_ID
3. Deploy.
4. Open /api/run in browser to test.
5. Cron is configured in vercel.json (every 20 minutes).
